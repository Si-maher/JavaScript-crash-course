const name = 'Simon'
const age = 41

// Template literals 

console.log(`My name is ${name} and I am ${age} years young`);

// A method is a function that is associated with an Object. It needs parenthesis 
// A property does not need parenthesis 

const sentence = 'Only the dead have seen war'
console.log(sentence.length);
console.log(sentence.toLocaleUpperCase());
console.log(sentence.substring(0,5));
console.log(sentence.split(''));
console.log(sentence.split(','));